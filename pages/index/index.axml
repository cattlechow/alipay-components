<view class="container">
 <view class="title">
   功能导航
 </view>
 <view class="list">
   <view class="item" a:for="{{list}}" a:for-item="item" data-url="{{item.url}}" onTap="navigateTo" key="{{item.id}}">
    {{item.title}}
  </view>
 </view>
</view>